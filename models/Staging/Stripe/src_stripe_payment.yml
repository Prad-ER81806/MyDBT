sources:
  - name: stripe_payment
    database: raw
    schema: stripe
    tables:
      - name: payment
        loaded_at_field: _BATCHED_AT
        freshness: 
          warn_after:
            count: 2
            period: day
          error_after:
            count: 2
            period: day
        
